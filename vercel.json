{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install",
  "buildCommand": "npx prisma generate --schema apps/api/prisma/schema.prisma && npm run build --workspaces --if-present",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api", "destination": "/api/index" },
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "no-store" }]
    }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 60,
      "includeFiles": "{apps/api/dist/**,node_modules/.prisma/client/**,node_modules/@prisma/client/**}"
    },
    "api/[...path].js": {
      "maxDuration": 60,
      "includeFiles": "{apps/api/dist/**,node_modules/.prisma/client/**,node_modules/@prisma/client/**}"
    }
  }
}
